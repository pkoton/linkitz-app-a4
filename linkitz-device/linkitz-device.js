// linkitz-device.js

function LinkitzDevice(logCallback, hexdumpCallback) {
//    this.bootloader = new MLABootloader(logCallback, hexdumpCallback);
    this.logCallback = logCallback;
    this.hexdumpCallback = hexdumpCallback;
}
